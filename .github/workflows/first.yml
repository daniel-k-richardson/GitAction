name: first

on: push

jobs:
  ubuntu-job: 
    name: ubuntu-job
    runs-on: ubuntu-latest
    steps:
      - name: Step one
        uses: actions/checkout@v2
      - name: json-lint
        uses: ocular-d/json-linter@0.0.2
